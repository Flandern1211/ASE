# 任务 16：完整测试

**目标：** 运行全部单元测试，修复所有编译和测试问题。

**文件：** 无（仅测试任务）

---

## 步骤

- [ ] **步骤 1：运行完整测试套件**

```bash
go test ./... -v -count=1 2>&1 | head -100
```
预期：所有单元测试通过。如果需要 Docker 的测试不可用，可用 `-short` 跳过。

- [ ] **步骤 2：修复编译和测试失败**

如果存在测试失败，修复根因（导入路径、类型不匹配、缺失方法），然后重新运行。

- [ ] **步骤 3：运行 go vet**

```bash
go vet ./...
```
预期：无警告

- [ ] **步骤 4：提交修复（如有）**

```bash
git add -A
git commit -m "fix: 修复编译和测试问题"
```
仅在有需要提交的修复时执行。
